# MongoDB Connection
# Option 1: MongoDB Atlas (Cloud - Recommended for development)
# Get your connection string from: https://cloud.mongodb.com/
# MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/student-chatbot?retryWrites=true&w=majority

# Option 2: Local MongoDB
# Install MongoDB locally and start the service, then use:
MONGO_URI=mongodb://localhost:27017/student-chatbot

# JWT Secrets (IMPORTANT: Change these in production!)
# Generate random secrets using: openssl rand -base64 32
JWT_ACCESS_SECRET=change_this_to_a_random_secret_string_for_access_tokens
JWT_REFRESH_SECRET=change_this_to_a_random_secret_string_for_refresh_tokens

# Google Gemini AI Configuration (Optional)
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_URL=https://generativelanguage.googleapis.com/v1/models/gemini-1.5-pro-latest:generateContent
GEMINI_API_KEY=your_gemini_api_key_here

# Redis Configuration (Optional - for rate limiting and caching)
# Uncomment if you have Redis installed and running
# REDIS_URL=redis://localhost:6379

# Admin API Key (Server-only secret)
# Generate a strong random string for this
ADMIN_API_KEY=change_this_to_a_strong_random_admin_key

# Server Configuration
PORT=5000
NODE_ENV=development
